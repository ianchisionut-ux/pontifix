"use client";
import { useEffect,useState } from "react";import { Cloud,Download,Link2,RefreshCw,Unlink } from "lucide-react";
type Status={configured:boolean;connected:boolean;environment:"test"|"production";redirectUri:string;connection?:{expiresAt:string;connectedAt:string}};type Message={id:number;messageId:string;direction:"RECEIVED"|"SENT";cif:string;details:string;documentDate:string;downloadId:string;createdAt:string};
export default function EFacturaPage(){const[status,setStatus]=useState<Status|null>(null),[messages,setMessages]=useState<Message[]>([]),[busy,setBusy]=useState(false),[error,setError]=useState("");async function load(){const[s,m]=await Promise.all([fetch("/api/accounting/efactura/status").then(r=>r.json()),fetch("/api/accounting/efactura/messages").then(r=>r.json())]);setStatus(s);setMessages(Array.isArray(m)?m:[]);}useEffect(()=>{load();const params=new URLSearchParams(window.location.search);const callbackError=params.get("error");if(callbackError)setError(callbackError);if(params.has("error")||params.has("connected"))window.history.replaceState({},"",window.location.pathname);},[]);async function sync(){setBusy(true);setError("");const r=await fetch("/api/accounting/efactura/sync",{method:"POST"});const d=await r.json();setBusy(false);if(!r.ok)setError(d.error||"Sincronizarea a eșuat.");else load();}async function disconnect(){if(!confirm("Deconectezi contul ANAF?"))return;await fetch("/api/accounting/efactura/disconnect",{method:"POST"});load();}
return <div><div className="page-head"><div><div className="eyebrow">RO e-Factura · SPV</div><h2 className="page-title">Integrare ANAF</h2><p className="page-subtitle">Transmitere, statusuri și facturi primite prin API-ul oficial.</p></div>{status?.connected&&<button className="btn-secondary" onClick={sync} disabled={busy}><RefreshCw size={15}/>{busy?"Se sincronizează…":"Sincronizează SPV"}</button>}</div>{error&&<div className="ref-error">{error}</div>}
<div className="ef-status-grid"><div className="card"><div className="section-label">Configurare</div><div className="ef-status-line"><span>Mediu ANAF</span><strong className={status?.environment==="production"?"ef-prod":"ef-test"}>{status?.environment==="production"?"Producție":"Test"}</strong></div><div className="ef-status-line"><span>Client OAuth</span><strong>{status?.configured?"Configurat":"Lipsește"}</strong></div><div className="ef-status-line"><span>Conexiune SPV</span><strong>{status?.connected?"Conectată":"Neconectată"}</strong></div><div className="divider"/>{!status?.configured?<p className="page-subtitle">Adaugă în Vercel ANAF_CLIENT_ID, ANAF_CLIENT_SECRET, ANAF_REDIRECT_URI și ANAF_ENVIRONMENT.</p>:status.connected?<button className="btn-danger" onClick={disconnect}><Unlink size={14}/>Deconectează ANAF</button>:<a href="/api/accounting/efactura/connect" className="btn-primary"><Link2 size={14}/>Conectează certificatul/SPV</a>}<div className="ef-callback">Callback: {status?.redirectUri}</div></div>
<div className="card"><div className="section-label">Flux activ</div><div className="ef-flow"><Cloud size={28}/><p>Verificare preliminară locală → XML CIUS-RO → încărcare ANAF → verificare status → arhivare răspuns.</p></div><p className="page-subtitle">Facturile eligibile sunt trimise automat la emitere. O verificare zilnică urmărește răspunsurile ANAF și reîncearcă erorile temporare; controlul manual rămâne disponibil din fiecare factură.</p></div></div>
<div className="section-label">Mesaje e-Factura sincronizate</div><div className="card-table"><table><thead><tr><th>Direcție</th><th>ID ANAF</th><th>CIF</th><th>Detalii</th><th>Data</th><th></th></tr></thead><tbody>{messages.length===0?<tr><td colSpan={6} className="empty-row">Nu există mesaje sincronizate.</td></tr>:messages.map(m=><tr key={m.id}><td><span className={`badge ${m.direction==="RECEIVED"?"badge-partial":"badge-paid"}`}>{m.direction==="RECEIVED"?"Primită":"Trimisă"}</span></td><td className="num">{m.messageId}</td><td>{m.cif||"—"}</td><td>{m.details||"—"}</td><td>{m.documentDate||new Date(m.createdAt).toLocaleDateString("ro-RO")}</td><td><a className="btn-secondary" href={`/api/accounting/efactura/messages/${m.downloadId}/download`}><Download size={13}/>ZIP</a></td></tr>)}</tbody></table></div></div>}