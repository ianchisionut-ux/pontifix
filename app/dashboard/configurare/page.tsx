import { auth } from '@/lib/auth'
import { prisma } from '@/lib/prisma'
import { redirect } from 'next/navigation'
export const dynamic='force-dynamic'
export default async function SettingsPage(){const session=await auth();const businessId=(session as any)?.businessId;if(!businessId)redirect('/login');const business=await prisma.business.findUnique({where:{id:businessId},select:{name:true}});return <div className="p-4 lg:p-8 max-w-4xl mx-auto"><div className="mb-6"><h1 className="text-2xl font-semibold">Configurare</h1><p className="text-sm text-slate-500 mt-1">Regulile organizației și programul standard.</p></div><div className="grid gap-4"><section className="card p-6"><h2 className="font-semibold mb-4">Organizație</h2><div className="grid sm:grid-cols-2 gap-3"><label className="text-sm">Denumire<input className="input-field w-full mt-1" defaultValue={business?.name}/></label><label className="text-sm">Fus orar<input className="input-field w-full mt-1" value="Europe/Bucharest" readOnly/></label></div></section><section className="card p-6"><h2 className="font-semibold mb-2">Program standard</h2><p className="text-sm text-slate-500 mb-4">Norma implicită folosită pentru calculele de prezență și ore suplimentare.</p><div className="grid sm:grid-cols-3 gap-3"><label className="text-sm">Început<input className="input-field w-full mt-1" type="time" defaultValue="09:00"/></label><label className="text-sm">Sfârșit<input className="input-field w-full mt-1" type="time" defaultValue="17:30"/></label><label className="text-sm">Pauză<input className="input-field w-full mt-1" type="number" defaultValue="30"/></label></div></section></div></div>}
