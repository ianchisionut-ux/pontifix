import { NextResponse } from "next/server";import { accountingApi } from "@/lib/accounting/access";import { getInvoiceFull } from "@/lib/accounting/repo";import { validateEFactura,latestSubmission } from "@/lib/accounting/efactura";
async function GETHandler(_r:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const full=await getInvoiceFull(Number(id));if(!full||!full.client)return NextResponse.json({error:"Factura nu există."},{status:404});const errors=validateEFactura(full);return NextResponse.json({valid:errors.length===0,errors,submission:await latestSubmission(Number(id))});}export const GET=accountingApi(GETHandler);