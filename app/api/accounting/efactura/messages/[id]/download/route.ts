import { NextResponse } from "next/server";import { accountingApi } from "@/lib/accounting/access";import { downloadAnafMessage } from "@/lib/accounting/efactura";
async function GETHandler(_r:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const file=await downloadAnafMessage(id);return new NextResponse(file.buffer as unknown as BodyInit,{headers:{"Content-Type":file.contentType,"Content-Disposition":`attachment; filename="eFactura_ANAF_${id}.zip"`}});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Descărcarea a eșuat."},{status:400});}}export const GET=accountingApi(GETHandler);