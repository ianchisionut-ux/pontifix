import { NextRequest,NextResponse } from "next/server"; import { exchangeAnafCode } from "@/lib/accounting/efactura";
export async function GET(req:NextRequest){const code=req.nextUrl.searchParams.get("code"),state=req.nextUrl.searchParams.get("state"),expected=req.cookies.get("anaf_oauth_state")?.value; const target=new URL("/dashboard/contabilitate/efactura",req.url); if(!code||!state||state!==expected){target.searchParams.set("error","Răspuns OAuth ANAF invalid sau expirat.");return NextResponse.redirect(target);} try{await exchangeAnafCode(code);target.searchParams.set("connected","1");}catch(e){target.searchParams.set("error",e instanceof Error?e.message:"Conectarea ANAF a eșuat.");}const response=NextResponse.redirect(target);response.cookies.delete("anaf_oauth_state");return response;}