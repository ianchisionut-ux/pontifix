import { NextResponse } from 'next/server'
import { get } from '@vercel/blob'
import { auth } from '@/lib/auth'
import { prisma } from '@/lib/prisma'
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const session=await auth();const businessId=(session as any)?.businessId as string|undefined;if(!businessId)return NextResponse.json({error:'Neautorizat'},{status:401});const {id}=await params;const project=await prisma.project.findFirst({where:{id,businessId},select:{documentUrl:true,documentName:true}});if(!project?.documentUrl)return NextResponse.json({error:'Document inexistent.'},{status:404});const result=await get(project.documentUrl,{access:'private'});if(!result?.stream)return NextResponse.json({error:'Fișier indisponibil.'},{status:404});const headers=new Headers();headers.set('Content-Type','application/pdf');headers.set('Content-Disposition',`inline; filename="${(project.documentName||'certificat.pdf').replace(/"/g,'')}"`);return new Response(result.stream,{headers})}