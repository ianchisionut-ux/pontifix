"use client";import { useEffect,useState } from "react";import { CheckCircle2,CloudUpload,Code2,RefreshCw,TriangleAlert } from "lucide-react";
type Validation={valid:boolean;errors:string[];submission:null|{id:number;status:string;message:string;uploadId:string;submittedAt:string}};
export function EFacturaPanel({invoiceId}:{invoiceId:number}){const[data,setData]=useState<Validation|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");function load(){fetch(`/api/accounting/efactura/invoices/${invoiceId}/validate`).then(r=>r.json()).then(setData)}useEffect(load,[invoiceId]);async function send(){if(!confirm("Trimiți factura la ANAF în mediul configurat?"))return;setBusy(true);setError("");const r=await fetch(`/api/accounting/efactura/invoices/${invoiceId}/send`,{method:"POST"});const d=await r.json();setBusy(false);if(!r.ok)setError(d.error||"Transmiterea a eșuat.");load()}async function check(){if(!data?.submission)return;setBusy(true);const r=await fetch(`/api/accounting/efactura/submissions/${data.submission.id}/status`,{method:"POST"});const d=await r.json();setBusy(false);if(!r.ok)setError(d.error||"Verificarea a eșuat.");load()}
return <div className="card ef-invoice-panel"><div className="flex items-center justify-between gap-3"><div><div className="section-label">RO e-Factura</div><div className="ef-validation">{data?.valid?<><CheckCircle2 size={16}/>Verificarea preliminară a datelor a trecut</>:<><TriangleAlert size={16}/>Factura necesită completări</>}</div></div>{data?.submission&&<span className={`badge ${data.submission.status==="VALIDATED"?"badge-paid":data.submission.status==="REJECTED"?"badge-canceled":"badge-partial"}`}>{data.submission.status}</span>}</div>{data && data.errors.length > 0 && <ul className="ef-errors">{data.errors.map(e=><li key={e}>{e}</li>)}</ul>}{error&&<div className="ref-error">{error}</div>}<div className="flex gap-2 flex-wrap"><a className="btn-secondary" href={`/api/accounting/efactura/invoices/${invoiceId}/xml`}><Code2 size={14}/>Descarcă XML</a>{(!data?.submission||["REJECTED","ERROR"].includes(data.submission.status))&&<button className="btn-primary" onClick={send} disabled={!data?.valid||busy}><CloudUpload size={14}/>{busy?"Se procesează…":"Trimite la ANAF"}</button>}{data?.submission&&["PROCESSING","UPLOADING"].includes(data.submission.status)&&<button className="btn-secondary" onClick={check} disabled={busy}><RefreshCw size={14}/>Verifică status</button>}</div></div>}